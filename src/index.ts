import MainClassElement from "./MainClassElement";
import ClassElement from "./ClassElement";

/**
 * Insert HTML element to run html
 * @param htmlElement Main HTML element to be set on and run
 */
export function mainHTML(htmlElement: MainClassElement): void {
  
}
